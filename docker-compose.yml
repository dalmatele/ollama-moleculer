services:

  services:
    build:
      context: .
    image: moleculer-ollama
    env_file: docker-compose.env
    environment:
      PORT: 80
      OLLAMA_HOST: http://localhost:11434
    network_mode: host
    restart: always
